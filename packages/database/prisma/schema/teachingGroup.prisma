model TeachingGroup {
  id          Int     @id @default(autoincrement())
  groupName   String
  groupBalance Float
  teacher     Teacher @relation(fields: [teacherId], references: [id])
  teacherId   Int
}