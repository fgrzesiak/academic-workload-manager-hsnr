//test
generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  output          = "../../dist"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}